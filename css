<style>
/* ═══════════════════════════════════════════════════════
   PO MANAGEMENT SYSTEM — CSS
   File: css.html   used via  <?!= include('css'); ?>
═══════════════════════════════════════════════════════ */

/* ── Base ── */
body {
  background: #f0f2f5;
  font-size: 14px;
}

/* ── Navbar ── */
.navbar {
  position: sticky;
  top: 0;
  z-index: 1030;
}

/* ── Tabs ── */
.nav-tabs .nav-link {
  font-weight: 500;
  color: #495057;
  border-radius: 6px 6px 0 0;
  transition: background .2s, color .2s;
}
.nav-tabs .nav-link:hover {
  background: #e9ecef;
  color: #0d6efd;
}
.nav-tabs .nav-link.active {
  font-weight: 700;
  color: #0d6efd;
  background: #fff;
  border-bottom-color: #fff;
}

/* ── Stat Cards ── */
.stat-card {
  transition: transform .2s, box-shadow .2s;
}
.stat-card:hover {
  transform: translateY(-3px);
  box-shadow: 0 6px 16px rgba(0,0,0,.15) !important;
}
.stat-label {
  font-size: 0.78rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: .04em;
  opacity: .88;
}
.stat-value {
  font-size: 2rem;
  font-weight: 700;
  line-height: 1.1;
  margin-top: 4px;
}

/* ── Cards ── */
.card {
  border: none;
  border-radius: 10px;
}
.card-header {
  border-radius: 10px 10px 0 0 !important;
  font-size: 0.875rem;
}
.section-header {
  background: #0d6efd;
  color: #fff;
  font-weight: 600;
  padding: .6rem 1rem;
}

/* ── Tables ── */
.table thead th {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: .04em;
  background: #f8f9fa;
  white-space: nowrap;
}
.table tbody tr:hover {
  background: #eef4ff;
}
.table td, .table th {
  vertical-align: middle;
}

/* ── Badges ── */
.badge-status {
  font-size: .75rem;
  padding: .35em .65em;
  border-radius: 20px;
}

/* ── Form Controls ── */
.form-control, .form-select {
  font-size: 13.5px;
  border-radius: 6px;
  border-color: #ced4da;
  transition: border-color .2s, box-shadow .2s;
}
.form-control:focus, .form-select:focus {
  border-color: #0d6efd;
  box-shadow: 0 0 0 .2rem rgba(13,110,253,.2);
}
.form-control[readonly] {
  background: #e9ecef;
  cursor: default;
}

/* ── Inline table inputs (PO items/charges rows) ── */
.po-row-input {
  border: 1px solid #dee2e6;
  border-radius: 5px;
  padding: 4px 7px;
  font-size: 13px;
  width: 100%;
  background: #fff;
  transition: border-color .2s;
}
.po-row-input:focus {
  outline: none;
  border-color: #0d6efd;
  box-shadow: 0 0 0 .15rem rgba(13,110,253,.2);
}
.po-row-input[readonly] {
  background: #f8f9fa;
  color: #6c757d;
}
.po-row-input.is-invalid {
  border-color: #dc3545;
}

/* ── Buttons ── */
.btn {
  font-size: 13.5px;
  border-radius: 6px;
}
.btn-delete-row {
  width: 30px;
  height: 30px;
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  font-size: 15px;
  line-height: 1;
}

/* ── Modals ── */
.modal-header.bg-primary .btn-close-white {
  filter: brightness(0) invert(1);
}
.modal-content {
  border: none;
  border-radius: 12px;
  overflow: hidden;
}

/* ── Action Modal pill tabs ── */
#actionTabs .nav-link {
  font-size: 0.82rem;
  border-radius: 20px;
  padding: .35rem .85rem;
}

/* ── Loading overlay ── */
#loadingOverlay {
  position: fixed;
  inset: 0;
  background: rgba(255,255,255,.75);
  z-index: 2000;
  display: none;
  align-items: center;
  justify-content: center;
}

/* ── Sticky table header ── */
.table-responsive thead.sticky-top th,
thead.sticky-top th {
  position: sticky;
  top: 0;
  z-index: 2;
}

/* ── Filter active card highlight ── */
.stat-card.filter-active {
  outline: 3px solid #fff;
  outline-offset: 2px;
  transform: translateY(-3px);
  box-shadow: 0 6px 16px rgba(0,0,0,.2) !important;
}

/* ── Responsive tweaks ── */
@media (max-width: 768px) {
  .stat-value { font-size: 1.5rem; }
  .container-fluid { padding-left: 12px; padding-right: 12px; }
  #poItemsTable th:nth-child(7),
  #poItemsTable td:nth-child(7) { display: none; }
}
</style>
